// Find range of char datatype;
#include <stdio.h>
#include <limits.h>

int main() {
    char minChar_value = CHAR_MIN;
    char maxChar_value = CHAR_MAX;
    printf("Minimum value of character datatype stores: %d\n", minChar_value);
    printf("Maximum value of character datatype stores: %d\n", maxChar_value);

    return 0;
}